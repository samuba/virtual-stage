{
  "name": "virtual-office",
  "version": "0.0.1",
  "description": "Feel as your remote workers are right in your room without being to intrusive",
  "main": "server.js",
  "scripts": {
    "start": "node server.js && yarn --cwd ./frontend serve",
    "build": "yarn --cwd ./frontend build && yarn --cwd ./frontend cleanup"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "12.x"
  }
}
